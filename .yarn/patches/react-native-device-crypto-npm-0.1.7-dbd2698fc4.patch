diff --git a/android/build.gradle b/android/build.gradle
index 8eb758ed063d1905f538f72e3ee55a8c41e01d18..d1ee770a3594017357465e031c2edb4aae6a448c 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -3,7 +3,6 @@ buildscript {
         repositories {
             google()
             mavenCentral()
-            jcenter()
         }
 
         dependencies {
@@ -52,7 +51,6 @@ repositories {
     }
     google()
     mavenCentral()
-    jcenter()
 }
 
 dependencies {
diff --git a/android/src/main/java/com/reactnativedevicecrypto/Helpers.java b/android/src/main/java/com/reactnativedevicecrypto/Helpers.java
index 03fd79e8a389f33a2841ecf0e177ffc9ea01b78c..d903b5dc4b0110dab9a8c1a648d349200dd4e20f 100644
--- a/android/src/main/java/com/reactnativedevicecrypto/Helpers.java
+++ b/android/src/main/java/com/reactnativedevicecrypto/Helpers.java
@@ -39,6 +39,7 @@ public class Helpers {
     private static final int AES_IV_SIZE = 128;
     public static final String PEM_HEADER = "-----BEGIN PUBLIC KEY-----\n";
     public static final String PEM_FOOTER = "-----END PUBLIC KEY-----";
+    private static Boolean sIsStrongBoxSupported = null; // Cache for StrongBox support
 
     public interface KeyType {
         @Retention(SOURCE)
@@ -81,6 +82,64 @@ public class Helpers {
         }
     }
 
+    public static boolean isStrongBoxSupported() {
+        // Return cached result if available
+        if (sIsStrongBoxSupported != null) {
+            return sIsStrongBoxSupported;
+        }
+
+        // Try to create a key with StrongBox requirement and see if it succeeds
+        String testAlias = "test_strongbox_support";
+        try {
+            // First, clean up any existing test key
+            try {
+                KeyStore keyStore = getKeyStore();
+                if (keyStore.containsAlias(testAlias)) {
+                    keyStore.deleteEntry(testAlias);
+                }
+            } catch (Exception e) {
+                // Ignore errors during cleanup
+            }
+
+            // Attempt to create a key with StrongBox backing
+            KeyGenParameterSpec testSpec = new KeyGenParameterSpec.Builder(testAlias, KeyProperties.PURPOSE_ENCRYPT)
+                    .setIsStrongBoxBacked(true)
+                    .setBlockModes(KeyProperties.BLOCK_MODE_GCM)
+                    .setEncryptionPaddings(KeyProperties.ENCRYPTION_PADDING_NONE)
+                    .setKeySize(256)
+                    .build();
+            KeyGenerator testKeyGen = KeyGenerator.getInstance(KeyProperties.KEY_ALGORITHM_AES, KEY_STORE);
+            testKeyGen.init(testSpec);
+            testKeyGen.generateKey();
+
+            // If we reach here, StrongBox is supported
+            // Clean up the test key
+            KeyStore keyStore = getKeyStore();
+            keyStore.deleteEntry(testAlias);
+
+            // Cache the result
+            sIsStrongBoxSupported = true;
+            Log.i(RN_MODULE, "StrongBox is supported on this device");
+            return true;
+        } catch (Exception e) {
+            Log.i(RN_MODULE, "StrongBox not supported on this device: " + e.getMessage());
+
+            // Clean up any test key that might have been created
+            try {
+                KeyStore keyStore = getKeyStore();
+                if (keyStore.containsAlias(testAlias)) {
+                    keyStore.deleteEntry(testAlias);
+                }
+            } catch (Exception cleanupEx) {
+                // Ignore errors during cleanup
+            }
+
+            // Cache the result
+            sIsStrongBoxSupported = false;
+            return false;
+        }
+    }
+
     public static boolean isKeyExists(@NonNull String alias, @KeyType.Types int keyType) throws Exception {
         KeyStore keyStore = Helpers.getKeyStore();
         if (!keyStore.containsAlias(alias)) {
@@ -134,17 +193,22 @@ public class Helpers {
           case AccessLevel.AUTHENTICATION_REQUIRED:
             // Sets whether this key is authorized to be used only if the user has been authenticated.
             builder.setUserAuthenticationRequired(true);
+
             // Allow pin/pass as a fallback on API 30+
-            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
-              builder.setUserAuthenticationParameters(0, KeyProperties.AUTH_DEVICE_CREDENTIAL | KeyProperties.AUTH_BIOMETRIC_STRONG);
-            }
+            // Disabled this as it prevents invalidation of the user removes the biometric
+            // if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+            //   builder.setUserAuthenticationParameters(0, KeyProperties.AUTH_DEVICE_CREDENTIAL | KeyProperties.AUTH_BIOMETRIC_STRONG);
+            // }
+
             // Invalidate the keys if the user has registered a new biometric
             // credential. The variable "invalidatedByBiometricEnrollment" is true by default.
             if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.N) {
               builder.setInvalidatedByBiometricEnrollment(invalidateOnNewBiometry);
             }
-            if (Build.VERSION.SDK_INT > Build.VERSION_CODES.R) {
-              builder.setIsStrongBoxBacked(true);
+            // Only try to enable StrongBox if API level is high enough and the device
+            // actually supports it
+            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.P && isStrongBoxSupported()) {
+                builder.setIsStrongBoxBacked(true);
             }
             break;
         }
diff --git a/ios/DeviceCrypto.m b/ios/DeviceCrypto.m
index 1c05886476b28afcb44eb8fc36f5141593d41b0e..44f7950d2f058f3093d1294d85074d9b9f4324a4 100644
--- a/ios/DeviceCrypto.m
+++ b/ios/DeviceCrypto.m
@@ -1,6 +1,7 @@
 #import <Security/Security.h>
 #import <React/RCTConvert.h>
 #import <React/RCTBridge.h>
+#import <React/RCTLog.h>
 #import <React/RCTUtils.h>
 #import <LocalAuthentication/LAContext.h>
 #import <LocalAuthentication/LAError.h>
@@ -33,46 +34,39 @@ typedef NS_ENUM(NSUInteger, AccessLevel) {
 
 - (SecKeyRef) getPublicKeyRef:(nonnull NSData*) alias
 {
-  NSDictionary *query = @{
-    (id)kSecClass:               (id)kSecClassKey,
-    (id)kSecAttrKeyClass:        (id)kSecAttrKeyClassPublic,
-    (id)kSecAttrLabel:           @"publicKey",
-    (id)kSecAttrApplicationTag:  (id)alias,
-    (id)kSecReturnRef:           (id)kCFBooleanTrue,
-  };
-  
-  CFTypeRef resultTypeRef = NULL;
-  OSStatus status = SecItemCopyMatching((__bridge CFDictionaryRef) query, &resultTypeRef);
-  if (status == errSecSuccess) {
-    return (SecKeyRef)resultTypeRef;
-  } else if (status == errSecItemNotFound) {
-    return nil;
-  } else
-  [NSException raise:@"Unexpected OSStatus" format:@"Status: %i", (int)status];
-  return nil;
+  SecKeyRef privateKey = [self getPrivateKeyRef:alias withMessage:nil];
+  if (!privateKey) {
+    NSError *error = [NSError errorWithDomain:NSOSStatusErrorDomain code:errSecItemNotFound userInfo:nil];
+    @throw [NSException exceptionWithName:@"E_PUBLIC_KEY_PRIVATE_MISSING"
+                                  reason:@"The private wrapping key was not found."
+                                userInfo:@{@"error": error}];
+  }
+
+  // An independently stored public key can outlive its matching private key after restore.
+  SecKeyRef publicKey = SecKeyCopyPublicKey(privateKey);
+  CFRelease(privateKey);
+  if (!publicKey) {
+    @throw [NSException exceptionWithName:@"E_PUBLIC_KEY_DERIVATION"
+                                  reason:@"Could not obtain the public key for the private wrapping key."
+                                userInfo:nil];
+  }
+  return publicKey;
 }
 
 - (NSData *) getPublicKeyBits:(nonnull NSData*) alias
 {
-  NSDictionary *query = @{
-    (id)kSecClass:               (id)kSecClassKey,
-    (id)kSecAttrKeyClass:        (id)kSecAttrKeyClassPublic,
-    (id)kSecAttrLabel:           @"publicKey",
-    (id)kSecAttrApplicationTag:  (id)alias,
-    (id)kSecReturnData:          (id)kCFBooleanTrue,
-    (id)kSecReturnRef:           (id)kCFBooleanTrue,
-  };
-  
-  SecKeyRef keyRef;
-  OSStatus status = SecItemCopyMatching((__bridge CFDictionaryRef) query, (CFTypeRef *)&keyRef);
-  if (status == errSecSuccess) {
-    return CFDictionaryGetValue((CFDictionaryRef)keyRef, kSecValueData);
-  } else if (status == errSecItemNotFound) {
-    return nil;
-  } else {
-    [NSException raise:@"Unexpected OSStatus" format:@"Status: %i", status];
+  SecKeyRef publicKey = [self getPublicKeyRef:alias];
+  CFErrorRef error = NULL;
+  NSData *bits = CFBridgingRelease(SecKeyCopyExternalRepresentation(publicKey, &error));
+  CFRelease(publicKey);
+  if (!bits) {
+    NSError *nativeError = CFBridgingRelease(error);
+    @throw [NSException exceptionWithName:@"E_PUBLIC_KEY_EXPORT"
+                                  reason:@"Could not export the public wrapping key."
+                                userInfo:nativeError ? @{@"error": nativeError} : nil];
   }
-  return nil;
+  if (error) { CFRelease(error); }
+  return bits;
 }
 
 - (NSString *) getPublicKeyAsString:(nonnull NSData*) alias
@@ -112,29 +106,7 @@ - (NSString*) getPublicKeyAsPEM:(nonnull NSData*) alias
   return pemString;
 }
 
-- (bool) savePublicKeyFromRef:(nonnull SecKeyRef)publicKeyRef withAlias:(nonnull NSData*) alias
-{
-  NSDictionary* attributes =
-  @{
-    (id)kSecClass:              (id)kSecClassKey,
-    (id)kSecAttrKeyClass:       (id)kSecAttrKeyClassPublic,
-    (id)kSecAttrLabel:          @"publicKey",
-    (id)kSecAttrApplicationTag: (id)alias,
-    (id)kSecValueRef:           (__bridge id)publicKeyRef,
-    (id)kSecAttrIsPermanent:    (id)kCFBooleanTrue,
-  };
-  
-  OSStatus status = SecItemAdd((CFDictionaryRef)attributes, nil);
-  while (status == errSecDuplicateItem)
-  {
-    status = SecItemDelete((CFDictionaryRef)attributes);
-  }
-  status = SecItemAdd((CFDictionaryRef)attributes, nil);
-  
-  return true;
-}
-
-- (bool) deletePublicKey:(nonnull NSData*) alias
+- (void) deletePublicKey:(nonnull NSData*) alias
 {
   NSDictionary *query = @{
     (id)kSecClass:               (id)kSecClassKey,
@@ -143,11 +115,9 @@ - (bool) deletePublicKey:(nonnull NSData*) alias
     (id)kSecAttrApplicationTag:  (id)alias,
   };
   OSStatus status = SecItemDelete((CFDictionaryRef) query);
-  while (status == errSecDuplicateItem)
-  {
-    status = SecItemDelete((CFDictionaryRef) query);
+  if (status != errSecSuccess && status != errSecItemNotFound) {
+    RCTLogWarn(@"Legacy public wrapping-key cleanup failed (OSStatus %i).", (int)status);
   }
-  return true;
 }
 
 - (SecKeyRef) getPrivateKeyRef:(nonnull NSData*)alias withMessage:(NSString *)authPromptMessage
@@ -171,8 +141,12 @@ - (SecKeyRef) getPrivateKeyRef:(nonnull NSData*)alias withMessage:(NSString *)au
     return (SecKeyRef)resultTypeRef;
   else if (status == errSecItemNotFound)
     return nil;
-  else
-    [NSException raise:@"E1715: Unexpected OSStatus" format:@"Status: %i", (int)status];
+  else {
+    NSError *error = [NSError errorWithDomain:NSOSStatusErrorDomain code:status userInfo:nil];
+    @throw [NSException exceptionWithName:@"E1715: Unexpected OSStatus"
+                                  reason:[NSString stringWithFormat:@"Status: %i", (int)status]
+                                userInfo:@{@"error": error}];
+  }
   return nil;
 }
 
@@ -185,9 +159,11 @@ - (bool) deletePrivateKey:(nonnull NSData*) alias
     (id)kSecAttrApplicationTag:  (id)alias,
   };
   OSStatus status = SecItemDelete((CFDictionaryRef) query);
-  while (status == errSecDuplicateItem)
-  {
-    status = SecItemDelete((CFDictionaryRef) query);
+  if (status != errSecSuccess && status != errSecItemNotFound) {
+    NSError *error = [NSError errorWithDomain:NSOSStatusErrorDomain code:status userInfo:nil];
+    @throw [NSException exceptionWithName:@"E_DELETE_PRIVATE_KEY"
+                                  reason:[NSString stringWithFormat:@"Status: %i", (int)status]
+                                userInfo:@{@"error": error}];
   }
   return true;
 }
@@ -208,6 +184,7 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
 {
   SecKeyRef privateKeyRef = [self getPrivateKeyRef:alias withMessage:kAuthenticationRequired];
   if (privateKeyRef != nil) {
+    CFRelease(privateKeyRef);
     return [self getPublicKeyAsPEM:alias];
   }
 
@@ -244,7 +221,10 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
   SecAccessControlRef acRef = SecAccessControlCreateWithFlags(kCFAllocatorDefault, keyAccessLevel, acFlag, &error);
   
   if (!acRef) {
-    [NSException raise:@"E1711" format:@"Could not create access control."];
+    NSError *nativeError = CFBridgingRelease(error);
+    @throw [NSException exceptionWithName:@"E1711"
+                                  reason:[NSString stringWithFormat:@"Could not create access control: %@", nativeError]
+                                userInfo:nativeError ? @{@"error": nativeError} : nil];
   }
   
   NSDictionary* attributes =
@@ -265,11 +245,14 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
   };
   
   privateKeyRef = SecKeyCreateRandomKey((__bridge CFDictionaryRef)attributes, &error);
+  CFRelease(acRef);
   if (!privateKeyRef){
-    [NSException raise:@"E1712" format:@"SecKeyCreate could not create key."];
+    NSError *nativeError = CFBridgingRelease(error);
+    @throw [NSException exceptionWithName:@"E1712"
+                                  reason:[NSString stringWithFormat:@"SecKeyCreate could not create key: %@", nativeError]
+                                userInfo:nativeError ? @{@"error": nativeError} : nil];
   }
-  SecKeyRef publicKeyRef = SecKeyCopyPublicKey(privateKeyRef);
-  [self savePublicKeyFromRef:publicKeyRef withAlias:alias];
+  CFRelease(privateKeyRef);
   return [self getPublicKeyAsPEM:alias];
 }
 
@@ -288,16 +271,19 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
       resolve(publicKey != nil ? @(YES) : @(NO));
     }
   } @catch(NSException *err) {
-    reject(err.name, err.reason, nil);
+    reject(err.name, err.reason, err.userInfo[@"error"]);
   }
 }
 
 RCT_EXPORT_METHOD(deleteKey:(nonnull NSData *)alias resolver:(RCTPromiseResolveBlock)resolve rejecter:(RCTPromiseRejectBlock)reject)
 {
-  [self deletePublicKey:alias];
-  [self deletePrivateKey:alias];
-  
-  return resolve(@(YES));
+  @try {
+    [self deletePrivateKey:alias];
+    [self deletePublicKey:alias];
+    resolve(@(YES));
+  } @catch(NSException *err) {
+    reject(err.name, err.reason, err.userInfo[@"error"]);
+  }
 }
 
 RCT_EXPORT_METHOD(sign:(nonnull NSData *)alias withPlainText:(nonnull NSString *)plainText withOptions:(nonnull NSDictionary *)options resolver:(RCTPromiseResolveBlock)resolve rejecter:(RCTPromiseRejectBlock)reject)
@@ -357,7 +343,7 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
       @"encryptedText": [cipherText base64EncodedStringWithOptions:0],
     });
   } @catch(NSException *err) {
-    reject(err.name, err.description, nil);
+    reject(err.name, err.description, err.userInfo[@"error"]);
   }
 }
 
@@ -369,6 +355,12 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
     NSData *textToBeDecrypted = [[NSData alloc] initWithBase64EncodedString:plainText options:0];
     NSString *authMessage = options[kAuthenticatePrompt];
     SecKeyRef privateKeyRef = [self getPrivateKeyRef:alias withMessage:authMessage];
+    if (!privateKeyRef) {
+      NSError *error = [NSError errorWithDomain:NSOSStatusErrorDomain code:errSecItemNotFound userInfo:nil];
+      reject(@"E_PRIVATE_KEY_MISSING", @"The private wrapping key was not found.", error);
+      return;
+    }
+
     
     BOOL canDecrypt = SecKeyIsAlgorithmSupported(privateKeyRef, kSecKeyOperationTypeDecrypt, kSecKeyAlgorithmECIESEncryptionCofactorX963SHA256AESGCM);
     
@@ -383,7 +375,10 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
                                                                      &aerr));
     
     if (!clearText || aerr) {
-      [NSException raise:@"E1760 - Decryption error." format:@"%@", aerr];
+      NSError *nativeError = CFBridgingRelease(aerr);
+      @throw [NSException exceptionWithName:@"E1760 - Decryption error."
+                                    reason:[NSString stringWithFormat:@"%@", nativeError]
+                                  userInfo:nativeError ? @{@"error": nativeError} : nil];
     }
     
     if (privateKeyRef) { CFRelease(privateKeyRef); }
@@ -391,7 +386,7 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
     
     resolve([[NSString alloc] initWithData:clearText   encoding:NSUTF8StringEncoding]);
   } @catch(NSException *err) {
-    reject(err.name, err.description, nil);
+    reject(err.name, err.description, err.userInfo[@"error"]);
   }
 }
 
@@ -399,7 +394,11 @@ - (NSString*) getOrCreateKey:(nonnull NSData*) alias withOptions:(nonnull NSDict
 // ______________________________________________
 RCT_EXPORT_METHOD(getPublicKey:(nonnull NSData *)alias resolver:(RCTPromiseResolveBlock)resolve rejecter:(RCTPromiseRejectBlock)reject)
 {
-  return resolve([self getPublicKeyAsPEM:alias]);
+  @try {
+    resolve([self getPublicKeyAsPEM:alias]);
+  } @catch(NSException *err) {
+    reject(err.name, err.reason, err.userInfo[@"error"]);
+  }
 }
 
 RCT_EXPORT_METHOD(isKeyExists:(nonnull NSData *)alias withKeyType:(nonnull NSNumber *) keyType resolver:(RCTPromiseResolveBlock)resolve rejecter:(RCTPromiseRejectBlock)reject)
diff --git a/lib/commonjs/index.js b/lib/commonjs/index.js
index d02926abfdf1d0cbeea5ab109c3239d686eb5d69..a3ab9892e68875dd2930a73d100f4c9021cc4dbc 100644
--- a/lib/commonjs/index.js
+++ b/lib/commonjs/index.js
@@ -81,7 +81,7 @@ const DeviceCrypto = {
    * @return {Promise} Resolves to `true` when successful
    */
   async deleteKey(alias) {
-    return Boolean(RNDeviceCrypto.deleteKey(alias));
+    return Boolean(await RNDeviceCrypto.deleteKey(alias));
   },
 
   /**
diff --git a/lib/module/index.js b/lib/module/index.js
index d30feaf62eb060b99d274606ff52eb365884d808..264d784a1cf1b0c7a33b5a3dd67e2ed1bb00d1a9 100644
--- a/lib/module/index.js
+++ b/lib/module/index.js
@@ -69,7 +69,7 @@ const DeviceCrypto = {
    * @return {Promise} Resolves to `true` when successful
    */
   async deleteKey(alias) {
-    return Boolean(RNDeviceCrypto.deleteKey(alias));
+    return Boolean(await RNDeviceCrypto.deleteKey(alias));
   },
 
   /**
diff --git a/src/index.ts b/src/index.ts
index d19d2d3840fafe69d02bf407603d37824a3adcb5..94ade5fee2148ce261db1c0da4484253c97d665d 100644
--- a/src/index.ts
+++ b/src/index.ts
@@ -85,7 +85,7 @@ const DeviceCrypto = {
    * @return {Promise} Resolves to `true` when successful
    */
   async deleteKey(alias: string): Promise<boolean> {
-    return Boolean(RNDeviceCrypto.deleteKey(alias));
+    return Boolean(await RNDeviceCrypto.deleteKey(alias));
   },
 
   /**
