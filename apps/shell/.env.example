# Account App URL (for iframe embedding)
NEXT_PUBLIC_ACCOUNT_APP_URL=/account-app

# WalletConnect Project ID (required for WalletConnect)
# Get one at https://cloud.walletconnect.com
NEXT_PUBLIC_WC_PROJECT_ID=

# Infura API token (optional, for RPC fallbacks)
NEXT_PUBLIC_INFURA_TOKEN=
