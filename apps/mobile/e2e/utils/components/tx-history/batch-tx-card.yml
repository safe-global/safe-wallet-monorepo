appId: ${APP_ID}
tags:
  - util
---
- runScript:
    file: ../../../utils/scripts/defaults.js
# ===========================================
# Batch Transaction Card
# ===========================================
# Find and assert batch transaction is visible
- assertVisible:
    text: 'Batch'

- extendedWaitUntil:
    visible:
      text: '.*\d+\s+actions.*'
      optional: true
<<<<<<< HEAD:apps/mobile/e2e/utils/components/tx-history/batch-tx-card.yml
    timeout: ${output.defaults.extendedWaitUntilTimeout}
=======
    timeout: 2000
>>>>>>> origin/main:apps/mobile/e2e/components/tx-history/batch-tx-card.yml

# Click on the batch transaction (or a sub-transaction within it)
# MultiSend transactions show as "Batch" with "X actions"
- tapOn:
    text: 'Batch'

- runFlow:
    file: ./batch-tx.yml

- tapOn:
    id: 'go-back'

# Verify we're back to the transaction list
- assertVisible:
    id: 'tx-history-list'
