import { tokens } from '@/src/theme/tokens'

export const approvalEditorTheme = {
  light_approval_info: {
    background: tokens.color.infoBackgroundLight,
    color: tokens.color.infoDarkLight,
    accent: tokens.color.infoMainLight,
  },
  dark_approval_info: {
    background: tokens.color.infoBackgroundDark,
    color: tokens.color.infoDarkDark,
    accent: tokens.color.infoMainDark,
  },
  light_approval_warning: {
    background: tokens.color.warningBackgroundLight,
    color: tokens.color.warningDarkLight,
    accent: tokens.color.warningMainLight,
  },
  dark_approval_warning: {
    background: tokens.color.warningBackgroundDark,
    color: tokens.color.warningDarkDark,
    accent: tokens.color.warningMainDark,
  },
}
