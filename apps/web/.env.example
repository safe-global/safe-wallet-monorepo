NEXT_PUBLIC_INFURA_TOKEN=1234567890
NEXT_PUBLIC_WC_PROJECT_ID=0987654321
NEXT_PUBLIC_IS_OFFICIAL_HOST=false
NEXT_PUBLIC_PROD_MIXPANEL_TOKEN=1234567890 # Use Mixpanel Project TEST here
NEXT_PUBLIC_STAGING_MIXPANEL_TOKEN=1234567890 # Use Mixpanel Project TEST here, too

# --- Safenet read layer -------------------------------------------------------
# Chain the Safenet Consensus contract lives on. Feeds BOTH the reader's provider
# network AND the EIP-712 domain used to derive oracle request ids — a wrong value
# silently derives wrong request ids. Defaults to Gnosis ('100').
NEXT_PUBLIC_SAFENET_CHAIN_ID=100
# Comma-separated, pinned Gnosis RPC endpoints for the read layer (rotated on failure).
NEXT_PUBLIC_SAFENET_RPC_URLS=https://rpc.gnosischain.com
# Safenet Consensus contract. Defaults to the Gnosis beta deployment.
NEXT_PUBLIC_SAFENET_CONSENSUS_ADDRESS=0x223624cBF099e5a8f8cD5aF22aFa424a1d1acEE9
# FROSTCoordinator the epoch group keys are read from. Defaults to the Gnosis beta deployment.
NEXT_PUBLIC_SAFENET_COORDINATOR_ADDRESS=0xaE27021CEB45316f1efe69D8E362aC07ED3Bd7E4
# Sentinel-oracle allowlist (comma-separated). SECURITY-CRITICAL.
#
# `Consensus.proposeOracleTransaction` is permissionless and takes the oracle
# address as a caller argument, so the oracle named in a proposal event is
# attacker-chosen. The reader only reads oracle events from an address listed
# here — otherwise anyone could emit a fabricated `OracleResult(approved=false)`
# from their own contract and mark any Safe transaction as malicious.
#
# Empty means no sentinel oracle is trusted and the oracle path is skipped. That
# is correct today: live beta runs the validator attesters only, with no sentinel
# oracle deployed. Set this once one exists.
NEXT_PUBLIC_SAFENET_ORACLE_ADDRESSES=